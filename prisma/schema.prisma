model Package {
  id               Int      @id @default(autoincrement())
  title            String
  capacity         String
  price            String
  description      String?
  image            String?
  installationKits String?  // Changed from features to match what you want!
  createdAt        DateTime @default(now())
}
